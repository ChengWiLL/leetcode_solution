class Solution
{
public:
	uint32_t reverseBits(uint32_t n)
	{
		int bit=0;
		int result=0;
		vector<int> bitmap;
		while(bit!=32)
		{
			bitmap.push_back(n%2);
			bit++;
			n/=2;
		}
		int tag=bit;
		for(int i=0;i<bit;++i)
		{
			result+=bitmap[i]*(1<<(--tag));
		}
		return result;
	}
};
